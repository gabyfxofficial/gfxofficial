import React from "react";
import ReactDOM from "react-dom/client";
// entry for the refreshed UI/UX 2025
import App from "./App";
import "./styles/index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <App />
  </React.StrictMode>
);
